name: Bench

on:
  push:
    branches: [main]
    paths:
      - "include/**"
      - "src/**"
      - "bench/**"
      - "cmake/**"
      - "CMakeLists.txt"
      - "CMakePresets.json"
      - "vcpkg.json"
      - "vcpkg-configuration.json"
      - ".github/workflows/bench.yml"
  pull_request:
    branches: [main]
    paths:
      - "include/**"
      - "src/**"
      - "bench/**"
      - "cmake/**"
      - "CMakeLists.txt"
      - "CMakePresets.json"
      - "vcpkg.json"
      - "vcpkg-configuration.json"
      - ".github/workflows/bench.yml"
  workflow_dispatch: {}

permissions:
  contents: read

concurrency:
  group: bench-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  microbench:
    name: Google Benchmark microbench
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Harden runner
        uses: step-security/harden-runner@ab7a9404c0f3da075243ca237b5fac12c98deaa5 # v2.19.3
        with:
          egress-policy: audit

      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Build the benchmark
        uses: ./.github/actions/cmake-preset
        with:
          preset: linux-clang-rel
          target: lob_bench

      - name: Run bench
        run: |
          set -euo pipefail
          mkdir -p artifacts
          ./build/linux-clang-rel/bench/lob_bench \
            --benchmark_format=json \
            --benchmark_min_time=0.5s \
            --benchmark_repetitions=5 \
            --benchmark_report_aggregates_only=true \
            --benchmark_out=artifacts/bench.json \
            --benchmark_out_format=json

      - name: Upload bench artefact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v4
        with:
          name: bench-json
          path: artifacts/bench.json
          retention-days: 30

      # benchmark-action/github-action-benchmark reads Google Benchmark's own
      # JSON, keeps the history in a file actions/cache carries between runs,
      # and writes the comparison against the previous run to the job summary.
      # It does not fail the job: consecutive runs land on different shared
      # runners, and the first comparison showed ratios of 1.2 to 2.9 on
      # unchanged code, which is the hardware, not the engine; a relative gate
      # needs a pinned host. The cache key ends in the run id and restores by
      # prefix, which is how actions/cache documents a cache that moves.
      - name: Restore the benchmark history
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v4
        with:
          path: ./cache
          key: benchmark-${{ runner.os }}-${{ github.run_id }}
          restore-keys: benchmark-${{ runner.os }}-

      - name: Record the run and compare with the previous one
        uses: benchmark-action/github-action-benchmark@52576c92bccf6ac60c8223ec7eb2565637cae9ba # v1.22.1
        with:
          name: lob_bench
          tool: googlecpp
          output-file-path: artifacts/bench.json
          external-data-json-path: ./cache/benchmark-data.json
          summary-always: true
          # A run on main writes the history; a pull request is compared
          # against it without moving it.
          save-data-file: ${{ github.event_name != 'pull_request' }}
